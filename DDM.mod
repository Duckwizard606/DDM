name = "Duck Demand Mod" # name of your mod, shown in launcher
path = "mod/DDM" # location of your mod files 
user_dir = "DDM" # optional. creates a sub directory where settings/map cache/savegames etc can be safely written for your mod and not get mixed up 
